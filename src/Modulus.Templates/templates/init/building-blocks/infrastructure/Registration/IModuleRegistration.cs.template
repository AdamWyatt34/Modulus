using Microsoft.AspNetCore.Routing;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;

namespace {{RootNamespace}}.BuildingBlocks.Infrastructure.Registration;

public interface IModuleRegistration
{
    IServiceCollection ConfigureServices(IServiceCollection services, IConfiguration configuration);

    IEndpointRouteBuilder ConfigureEndpoints(IEndpointRouteBuilder endpoints);
}
