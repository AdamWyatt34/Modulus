using System.Reflection;

namespace {{RootNamespace}}.{{ModuleName}}.Domain;

public static class AssemblyReference
{
    public static readonly Assembly Assembly = typeof(AssemblyReference).Assembly;
}
