<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- The tool always installs against the SDK's own .NET version (RollForward=LatestMajor
         below), so multi-targeting a dotnet-tool package buys nothing — single-target the
         current .NET 10 SDK; the scaffolds it emits are net10.0-only too. -->
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>modulus</ToolCommandName>
    <PackageId>ModulusKit.Cli</PackageId>
    <Description>CLI tool for scaffolding .NET modular monolith solutions with built-in CQRS mediator, messaging, and Aspire support.</Description>
    <PackageTags>dotnet-tool;modular-monolith;scaffolding;cqrs;aspire</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <MinVerTagPrefix>cli-v</MinVerTagPrefix>
    <MinVerDefaultPreReleaseIdentifiers>alpha.0</MinVerDefaultPreReleaseIdentifiers>
    <!-- Without this, `dotnet tool install -g ModulusKit.Cli` fails to launch on a machine whose
         only installed runtime is a newer major than the one this tool targets. -->
    <RollForward>LatestMajor</RollForward>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.CommandLine" />
    <PackageReference Include="MinVer" PrivateAssets="all" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" />
    <!-- Broker clients for `modulus dlq` (a self-contained tool package: these do not
         flow into any consumer's dependency graph). -->
    <PackageReference Include="RabbitMQ.Client" />
    <PackageReference Include="Azure.Messaging.ServiceBus" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Modulus.Templates\Modulus.Templates.csproj" />
    <ProjectReference Include="..\Modulus.Messaging\Modulus.Messaging.csproj" />
    <!-- Topology naming conventions for `modulus dlq`. -->
    <ProjectReference Include="..\Modulus.Messaging.RabbitMq\Modulus.Messaging.RabbitMq.csproj" />
    <ProjectReference Include="..\Modulus.Messaging.AzureServiceBus\Modulus.Messaging.AzureServiceBus.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
